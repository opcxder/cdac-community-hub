# Production Profile

# Logging (less verbose in production)
logging.level.com.cdac.admin=INFO

# Service URLs (use service names for Docker/Kubernetes deployment)
auth.service.url=http://localhost:8081
food.service.url=http://localhost:8086
hostel.service.url=http://localhost:8092
suggestion.service.url=http://localhost:8096

# Eureka Configuration (uses main config - enabled by default)
# Admin service will register with Eureka in production

