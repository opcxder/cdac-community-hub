spring.application.name=suggestion-service
server.port=8096

# Active Profile
spring.profiles.active=dev

# Database Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/suggestion_db
spring.datasource.username=root
spring.datasource.password=vanguard@1

# JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect
spring.jpa.properties.hibernate.format_sql=true

# Eureka Service Discovery
eureka.client.service-url.defaultZone=http://localhost:8761/eureka
eureka.client.register-with-eureka=true
eureka.client.fetch-registry=true
eureka.instance.prefer-ip-address=true
eureka.instance.hostname=suggestion-service

# Auth Service URL (for inter-service calls)
auth.service.url=http://localhost:8081

# JWT Configuration (must match auth-service secret)
jwt.secret=0bqJUqj+jkMXpDkYKrMG4WuptRKkc/nBe33Pq3euEd0=

# Logging Configuration
logging.level.root=INFO
logging.level.com.cdac.suggestion=DEBUG
logging.pattern.console=%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n
